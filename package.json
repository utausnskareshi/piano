{
  "name": "piano",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "3 octaves synthesizer PWA with WebAssembly DSP",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:wasm && npm run build:icons && tsc --noEmit && vite build",
    "build:wasm": "node scripts/build-wasm.mjs",
    "build:icons": "node scripts/build-icons.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vite-plugin-pwa": "^0.20.0",
    "sharp": "^0.33.0"
  }
}
